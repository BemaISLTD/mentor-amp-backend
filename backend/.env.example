# Neon direct connection (for Alembic migrations — NO "-pooler" in hostname)
DATABASE_URL=postgresql://user:password@ep-xxxx.us-east-1.aws.neon.tech/dbname?sslmode=require

# App settings
APP_NAME=MentorAmp
DEBUG=true
